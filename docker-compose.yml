version: "3.9"

services:
  verdaskills-frontend:
    container_name: verdaskills-frontend
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "5186:5186"

    restart: unless-stopped

    environment:
      NODE_ENV: production
      PORT: 5186

    env_file:
      - .env